cpu:
  architecture: 'X86_64'
  topology:
    cores: ${cores}
soundcard_enabled: False
timezone: 'Etc/GMT'
bios:
  boot_menu: True
type: server
template: Blank
disk_interface: virtio_scsi
cluster: st01